import React, { Component } from 'react';
import SideMenu from './sideMenu';

class SideMenuButton extends Component {
    state = {};
    render() {
        /*return (
            <React.Fragment>
                <button onClick={() => SideMenu.closeMenu()}>close</button>
                <button onClick={() => SideMenu.openMenu}>open</button>
            </React.Fragment>
        );*/
        return <></>;
    }
}

export default SideMenuButton;